# Deployment documentation

For a new installation, use [the deployment guide](deployment.md). Public repository configurations are local examples and do not select the hosted Canwoo installation.

Earlier versions of this document recorded the maintainer's initial deployment. Operational account configuration now lives outside Git. Dated validation notes elsewhere in this directory are historical records, not instructions to modify an existing service.
